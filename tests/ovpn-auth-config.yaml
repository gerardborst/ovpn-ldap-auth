ldapClient:
  base: "dc=example,dc=com"
  groupFilter: "(memberOf=%s)"
  host: "localhost"
  port: 389
  useSsl: false
  vpnGroupFilter: "(&(uid=%s)(memberOf=cn=group1,dc=example,dc=com))"
  serverName: "localhost"
  binddn: "cn=readonly,dc=example,dc=com"
  bindPassword: "readonly"
log:  
  level: "debug"
cn:
  check: false
  fail: false